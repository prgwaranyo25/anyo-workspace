# APH-4419

This folder holds the persistent working memory for Jira ticket `APH-4419`.

Use this template for either:
- a standalone ticket at the workspace root, or
- a child ticket nested under an epic folder

Single source of truth: `state.yml`
- `state.yml`: requirements, decisions, tasks, progress
- `scripts/`: helper commands (no secrets)
- `notes/`: short notes and checklists (no secrets)

Rules
- Never store secrets or PII.
- Update `progress.*` and task statuses after each iteration.
- Follow `../AGENTS.md` for the route investigation workflow.
